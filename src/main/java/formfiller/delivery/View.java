package formfiller.delivery;

/**
 * View outputs presented use case results to the user interface.
 */
public interface View {
	//	TODO:	Take a VM instead of a String.
	public void generateView(String message);
}
